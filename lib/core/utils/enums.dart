enum LocationFieldType { type, city, state }

enum DrawerSelected { home, account, help }

enum OrderTimeType { rightNow, schedule }

enum PaymentMethodType { cash, visaCard }

enum PromoCodeType { voucher , referral , test}

enum OrderStatus { onTheWay , onProgress , done , cancel}
